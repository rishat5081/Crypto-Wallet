version: '3.4'

services:
  binancesmartchain:
    image: binancesmartchain
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3010:3010
